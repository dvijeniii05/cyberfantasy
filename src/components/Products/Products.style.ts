import {ViewStyle} from 'react-native';

const parentContainer: ViewStyle = {
  backgroundColor: 'red',
  height: 100,
  marginTop: 20,
  marginHorizontal: 25,
};

export const styles = {
  parentContainer,
};
