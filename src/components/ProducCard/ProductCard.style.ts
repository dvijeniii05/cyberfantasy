import { ViewStyle } from "react-native";

const productContainer: ViewStyle = {
    backgroundColor: 'red',
    width: 100,
    marginHorizontal: 5,
    justifyContent: 'space-between',
  };

  export const styles = {
    productContainer
  }